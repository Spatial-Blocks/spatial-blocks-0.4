import React from 'react';

class Blocks extends React.Component {
    render() {
        return (
            <div className="blocks">
                {/* Drag-and-drop blocks for coding */}
            </div>
        );
    }
}

export default Blocks;
